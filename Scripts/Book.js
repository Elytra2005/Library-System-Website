

export class Book {
   constructor(title, id, year, author, genre, collection) {
       this.title = title,
       this.id = id,
       this.year = year,
       this.author = author,
       this.genre = genre,
       this.collection = collection,
       this.image = []
   }
}


